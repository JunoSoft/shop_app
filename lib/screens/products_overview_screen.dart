import 'package:flutter/material.dart';


class ProductsOverviewSCreen extends StatelessWidget {
  const ProductsOverviewSCreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('products over view'),
    );
  }
}
